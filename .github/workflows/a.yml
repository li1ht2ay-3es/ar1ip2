on:
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * *'
    - cron: '0 12 * * 0'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay-3es/ar1ip2@cron
        with:
          token: ${{ secrets.token }}
